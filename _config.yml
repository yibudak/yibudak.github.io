title: yibudak
email: yigit@outlook.com
name: Yiğit Budak
description: >- # this means to ignore newlines until "baseurl:"
  Python and Odoo Development Diaries
keywords: ['software', 'computer', 'development', 'python', 'odoo', 'web scraping', 'sql', 'linux']
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://yibudak.github.io" # the base hostname & protocol for your site, e.g. http://example.com
language: en
permalink: /:title
paginate: 25
paginate_path: /page/:num/

sass:
  sass_dir: _sass
  style: compressed # compressed, compact, expanded or compressed

# Category settings
jekyll-archives:
  enabled: 
    - categories
    - tags
  layout: archive
  permalinks:
    category: /category/:name/
    tag: /tag/:name/

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-archives
  - jekyll-seo-tag
  - jekyll-gist
